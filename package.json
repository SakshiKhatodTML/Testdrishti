{
  "name": "Drishti",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js ",
    "build": "npm run build",
    "runwebpack": "webpack --config webpack.prod.js -p",
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "eslint --fix ./"
  }
}
